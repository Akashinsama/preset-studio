# NOTICE · 来源与致谢

本仓库（工具与文档）在真实的第三方酒馆（SillyTavern）预设上开发、验证而来。
按作者要求，**仓库里不含任何预设正文**：条目内容、正则替换串、面板脚本里的原文一律未收录。
下面列出开发过程中参照、引用或以之为测试对象的作品，作为署名与致谢。

## 参照与致谢

| 作品 | 在工程里的位置 | 说明 |
|---|---|---|
| **芳乃预设**（原版） | 开发期的对比基准 | 本工程的成品预设是在它的 233 条条目基础上加面板脚本、正则与防截断段而来；**其条目正文属于原作者，未随本仓库分发**。 |
| **Ny 预设** | `⏩选项栏` 的写法 | 选项栏（`<options>` 四选项）这一用法源自该预设，工程内保留了这个出处注释。 |
| **Kemini Dramatron** | 防截断机制、思维链"多块"形态 | `tools/build-regex.mjs` 与面板的脚本层防截断参照其思路与形态重写（本仓库只含**自己写的实现**，不含其原文）。 |
| **Izumi** | 思维链"单块"形态、界面回归测试的样本 | 折叠链按"1 块走 Izumi 形态、≥2 块走 Kemini 形态"的规则统一；测试夹具是开发环境自备的，不入库。 |
| **梦鲸思客 V4** | 破甲层结构与分组推断的样本 | 同上，仅作开发期测试对象。 |

## 仓库里为什么看不到预设

1. 那些预设的著作权不在本仓库作者手里，公开转载需要各自作者的许可；
2. 预设正文和"工具能不能跑"是两件事——工具要的是**任意一份**结构化预设 JSON，
   你导入自己的那份就能用（见 `samples/README.md`）。

因此 `tools/make-fixture.mjs` 会照 `spec/` 里已经进库的结构信息（分组与成员名、槽位主人）
**自己造一份合成夹具**：结构与真实成品一致，正文全是占位，一个字节的第三方文本都没有。
一句话 `node tools/build-all.mjs` 之后，构建链与全部测试（实测 790 项）都能真跑。
人工准备的**真实**夹具永远优先：目标文件已存在且没有 `__synthFixture` 标记，生成器一个字都不动。

## 来源校验的口径变过一次（实话实说）

早先这个工程有一条硬约束：**"来源提示词一字未改"，并且由 `tools/check-preset.mjs`
拿三份源预设逐条哈希比对来证明**。合成夹具把这层意义改掉了：

| | 以前（真实夹具） | 现在（合成夹具 / 公开仓库） |
|---|---|---|
| `check-preset` 的来源比对 | 证明"成品里的条目与**那三份预设**逐字节一致" | 降级为**自证一致**：成品与**夹具**逐字节一致（仍然能抓住"组装过程篡改了内容"，但不再指向任何第三方作品） |
| `test-gui` 的规模数字 | 226 条 / 192 条在列表 / 60 条开启 / 30 条正则 | 按夹具自身重算，并打印"基准取自合成夹具" |
| 其余断言 | 不变 | 不变（结构类断言与夹具形状无关） |

这是作者明确接受的取舍：**放弃"逐字节证明来源未改"，换"克隆下来就能全跑"。**
拿真实夹具跑时，上表左列的行为原样恢复。

## 第三方依赖

本工程**零运行时依赖**：不联网、不装包、不用构建器。开发期的测试用到本机的
Node.js（跑 `.mjs` 脚本）与 Chrome/Edge（`check-*-browser.mjs` 的无头回归），
两者都不随仓库分发。压缩包里那份 `tools/lib/zip.mjs` 是自己写的最小 deflate 封装。

## 仓库里**确实**保留了哪些跟预设有关的东西

不含第三方正文 ≠ 一个字节都没有。为了让工具本身有意义，下面这些留下了，逐条说明白：

| 文件 | 里面是什么 | 为什么不删 |
|---|---|---|
| `spec/groups.json` | 模块/分组的**名字**、说明、成员条目名清单 | 面板的分组定义就是这些名字，删了面板与生成器都无从下手 |
| `preset/regex-manifest.json` | 四条思维链折叠正则的**模式**（本工程自己写的规则） | 它是"1 块 / ≥2 块"规则的规格，不含他人的替换串 |
| `panel/fano-panel.js`、`panel/src/` | 面板脚本本体（本工程自己写的） | 这是仓库的主角之一；里面只引用条目名，不含条目正文 |
| `spec/fano-layer.json`、`spec/mvu-layer.json` | **作者自己写的**芳乃层 / MVU 层提示词正文（最长一条 600 余字） | 这是构建链的输入，也是"这套工具做出来干什么"的答案。**如果你连自己这部分也不想公开，删掉这两个文件即可**——构建链会照常 SKIP，其余功能不受影响。 |
| `preset/build-report.txt` | 构建报告：条目名、槽位主人、计数 | 纯元数据，用来核对"来源一字未改" |

而以下这些**一律不在仓库里**（`.gitignore` 已挡）：三份源预设、成品预设与它的 zip、
`preset/fano-thinking-chain.json`（由源预设的 regex 替换串改写而来）、
`panel/preview-host.js`、`tools/gui/demo/*.js`、`inventory/` 抽取件、`dist/` 打包产物。

## 第三方依赖

本工程**零运行时依赖**：不联网、不装包、不用构建器。开发期的测试用到本机的
Node.js（跑 `.mjs` 脚本）与 Chrome/Edge（`check-*-browser.mjs` 的无头回归），
两者都不随仓库分发。压缩包里那份 `tools/lib/zip.mjs` 是自己写的最小 deflate 封装。

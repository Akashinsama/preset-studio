# NOTICE · 来源与致谢

本仓库（工具与文档）是在真实的第三方酒馆（SillyTavern）预设上开发、验证而来的。
作者定的口径只有一条：**可以大大方方承认借鉴，但不把别人的正文或代码当成自己的东西装出去。**

## 一、借了什么、怎么借的（分三类，界限很清楚）

### 1）条目名与结构 —— 借名字，作者明确接受这一层

| 文件 | 里面是什么 |
|---|---|
| `spec/groups.json` | 模块与分组的**名字**、说明、成员**条目名**清单 |
| `spec/slot-owner.json` | 内置槽位归哪一条条目（按名字） |
| `spec/groups-report.txt`、`spec/proposed-groups.md`、`preset/build-report.txt` | 分组/槽位推导过程的报告（只有名字与计数，没有正文） |
| `tools/make-fixture.mjs` | 按上面的结构造"结构等价、正文占位"的合成夹具（文件名沿用开发期那三个名字） |

为什么留着：面板的模块表、构建链与全部测试都以这些**名字**当接口。名字不是正文。

### 2）别人的代码 —— **全库只有两处**，都做成"可选"，默认都不装出去

| 出处 | 落在哪 | 默认 | 怎么打开 | 怎么删掉 |
|---|---|---|---|---|
| **Kemini Dramatron v3.1** 的 `scripts[0]`（作者 Kemini） | `panel/src/antitrunc.js`——脚本层防截断（经作者的芳乃预设 v2.8.1 原样移植；逐项对照见 `spec/防截断移植对照.md`） | **不注入**：默认那版面板里坐着的是**我们自己写的空壳** `panel/src/antitrunc-stub.js`，`CONFIG.antitrunc.enabled` 出厂也是 `false` | `node tools/build-panel.mjs --with-antitrunc`，或在编辑器「面板外观 → 脚本层防截断（可选注入）」里勾选 | 删掉 `panel/src/antitrunc.js` 即可：`build-panel` 从此只会出空壳版，编辑器那一项会自动变成不可勾，其余功能不受影响 |
| **Kemini Dramatron v3.1** 的 7 条正则（正文美化 3 / 防截断过滤 2 / 选项栏 2，**替换串原样保留**） | `preset/fano-regex-extra.json`（每条都带 `_source` 标注） | **不并入**成品预设 | `node tools/build-preset.mjs --with-extra-regex` | 删掉那个文件即可：构建链照常跑，只是成品少这 7 条规则 |

**若原作者主张权利，我们即删除。** 这两处之所以都做成"可选 + 默认关"，就是为了让"删掉"是一句话的事，
而不是要从一堆代码里往外剥。

默认那份面板（`panel/fano-panel.js`，库内产物）里**一个字节的第三方代码都没有**--
`tools/test-panel.mjs` 里有一条源码级断言盯着这件事（`emit_complete_response_` 不许出现在默认产物里）。

### 3）名字层面的提及

`panel/src/panel-core.js`、`tools/build-*.mjs` 与文档里会出现 Kemini / Izumi / 梦鲸思客 / 小此 这类名字：
它们要么是"这段能力是照着谁的做法学的"的注记，要么就是**借来的条目名**本身（见第 1 类）。
`spec/rename.json` 记着一条硬规矩：**三份源预设的提示词正文与原作者角色名一律原样保留，不做改名。**

## 二、仓库里的两张纸（都是我们自己的）

| 文件 | 是什么 | 谁生成 |
|---|---|---|
| `samples/标准纸.json` | **标准纸**：演示与测试都用这一张（27 条条目，正文全是「仅作演示，无实际作用」占位） | 库里这份就是纸本体；`tools/build-demo-paper.mjs` 把它转成编辑器用的那份 `.js` |
| `tools/gui/demo/paper-demo.js` | 编辑器双击打开时加载的演示数据（**随仓库走**，所以 clone 下来就有样例，不用先跑构建） | `node tools/build-demo-paper.mjs`（`--check` 可校验两者一致） |
| `preset/芳乃预设.json` | **成品预设**（作者自用那份）：构建产物，**不进库** | `node tools/build-preset.mjs` |

早先编辑器默认加载的是**一份第三方预设**（`demo/izumi-demo.js`），测试也拿那份纸当基线
（226 条 / 192 条在列表 / 60 条开启 / 30 条正则）。现在都换成了标准纸：
**纸是我们自己的，尺子才量得准**。那三份源预设仍然可以放进 `samples/` 当夹具跑真实数据——
真实夹具优先，合成夹具不覆盖它。

## 三、来源校验的口径变过一次（实话实说）

早先这个工程有一条硬约束：**"来源提示词一字未改"**，并由 `tools/check-preset.mjs`
拿三份源预设逐条哈希比对来证明。合成夹具把这层意义改掉了：

| | 以前（真实夹具） | 现在（合成夹具 / 公开仓库） |
|---|---|---|
| `check-preset` 的来源比对 | 证明"成品里的条目与**那三份预设**逐字节一致" | 降级为**自证一致**：成品与**夹具**逐字节一致（仍然能抓住"组装过程篡改了内容"，只是不再指向第三方作品） |
| 测试里的"规模"数字 | 226 条 / 60 条开启 / 30 条正则 写成基线 | **不再写死任何数字**：基准取自纸本身；"变量总线 / 互斥 / 标签族"这类**能力**改用测试里当场造的迷你样本验 |
| 其余断言 | 不变 | 不变（结构类断言与夹具形状无关） |

这是作者明确接受的取舍：**放弃"逐字节证明来源未改"，换"克隆下来就能全跑"。**
拿真实夹具跑时，上表左列的行为原样恢复。

## 四、仓库里**确实**保留了哪些跟预设有关的东西

不含第三方正文 ≠ 一个字节都没有。为了让工具本身有意义，下面这些留下了，逐条说明白：

| 文件 | 里面是什么 | 为什么不删 |
|---|---|---|
| `spec/groups.json` | 模块/分组的**名字**、说明、成员条目名清单 | 面板的分组定义就是这些名字，删了面板与生成器都无从下手 |
| `preset/regex-manifest.json` | 四条思维链折叠正则的**模式**（本工程自己写的规则） | 它是"1 块 / ≥2 块"规则的规格，不含他人的替换串。**只由真夹具重写**，合成夹具跑过不会覆盖它 |
| `panel/fano-panel.js`、`panel/src/` | 面板脚本本体（本工程自己写的；防截断那一段默认是空壳） | 这是仓库的主角之一；里面只引用条目名，不含条目正文 |
| `spec/fano-layer.json`、`spec/mvu-layer.json` | **作者自己写的**芳乃层 / MVU 层提示词正文（最长一条 600 余字） | 这是构建链的输入，也是"这套工具做出来干什么"的答案。**如果你连自己这部分也不想公开，删掉这两个文件即可**——构建链会照常 SKIP，其余功能不受影响 |
| `preset/build-report.txt` | 构建报告：条目名、槽位主人、计数 | 纯元数据，用来核对"来源一字未改"。同样**只由真夹具重写** |
| `preset/fano-regex-extra.json` | 借来的 7 条正则（见第 2 类） | 可选；删掉只是少 7 条规则 |

而以下这些**一律不在仓库里**（`.gitignore` 已挡）：三份源预设、成品预设与它的 zip、
`preset/fano-thinking-chain.json`（由源预设的 regex 替换串改写而来）、
`panel/preview-host.js`、`fano-demo.js` 之外的 `tools/gui/demo/*.js`、`inventory/` 抽取件、`dist/` 打包产物。

## 五、第三方依赖

本工程**零运行时依赖**：不联网、不装包、不用构建器。开发期的测试用到本机的
Node.js（跑 `.mjs` 脚本）与 Chrome/Edge（`check-*-browser.mjs` 的无头回归），
两者都不随仓库分发。压缩包里那份 `tools/lib/zip.mjs` 是自己写的最小 deflate 封装。

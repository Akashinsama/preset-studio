# 这张表很关键，别删。
#
# 这套工具的测试里有**逐字节一致**的断言（比如"空编辑导出后与导入的预设字节相同"，
# 靠 sha1 比对）。Git 默认的 core.autocrlf 会在签出时把 LF 换成 CRLF，
# 那会让这类断言在你机器上莫名其妙地失败——而失败原因跟你的改动毫无关系。
#
# 所以：一律不做行尾与编码转换，仓库里存什么字节，签出就是什么字节。
* -text

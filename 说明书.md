# 芳乃预设生成器 · 说明书

> 一句话：这是一个**零依赖的本地网页工具**，用来读懂、体检、编辑、可视化搭建酒馆（SillyTavern）
> 提示词预设；它把成品预设和它的悬浮窗面板一起打包，双击就能用，不联网、不上传、不改你的文件。
>
> 这份说明书有两部分：
> **第一部分**给人看（怎么打开、点哪里、怎么装进酒馆）；
> **第二部分**给 AI 看（怎么把这份代码当成库来调用，含可直接复制的配方）。
> 每个函数的完整清单在 `tools/gui/API.md`。

---

# 第一部分 · 给人看的

## 1. 30 秒上手

1. 双击包根目录的 **`开始.html`**（它会跳到编辑器），或者直接双击 `tools/gui/index.html`。
2. 页面会自动载入示例素材 **`preset/芳乃预设.json`**（本工程的成品预设）。
   想换成你自己的：右上角 **导入预设…**（可以多选）。
3. 第一次用就点左栏第一项 **引导：我要做什么** —— 那页按"你手上有什么、想改到哪一步"给清单。

不需要安装 Node、不需要服务器、不需要联网。用的是 `file://`，`<script src>` 能加载，
所有数据都在你本机的内存里；**页面不会自己写任何文件**，导出永远是"另存为"。

> 用 Chrome / Edge 打开最稳。若改了编辑器自己的代码，刷新时按 **Ctrl+F5**（页面没给 JS 加版本号）。
> 右上角的 **⟳ 刷新** 是另一回事：它**不重载页面**，只把当前这一屏重新挂一遍——页面发钝时点它解卡，
> 导入的预设、编辑草稿、输入框里打了一半的字都还在；但**改了 JS 文件它不会生效**，那种情况还得 Ctrl+F5。

## 2. 包里有什么

| 路径 | 是什么 | 能不能手改 |
|---|---|---|
| `开始.html` | 入口（跳到编辑器） | 可以，但没必要 |
| `tools/gui/index.html` | **编辑器本体**（双击就能用） | 别改 |
| `tools/gui/gui.css` | 编辑器自己的样式 | 随便改，改坏了不影响预设 |
| `tools/gui/lib/*.js` | **可编程的库**（8 个模块，见第二部分） | 别改 |
| `tools/gui/API.md` | 上面那 8 个模块的**完整函数清单**（给 AI 用） | 参考 |
| `tools/gui/demo/fano-demo.js` | 编辑器自动载入的**示例素材**（就是芳乃预设） | 由脚本生成 |
| `tools/gui/demo/fano-panel-demo.js` | 面板源码快照（面板外观页靠它预览） | 由脚本生成 |
| `tools/gui/demo/preview-host-demo.js` | 假酒馆 API（"导出独立预览页"时才加载） | 由脚本生成 |
| `preset/芳乃预设.json` | **成品预设**（233 条条目，52 条开启，内嵌面板脚本） | 这是产物，别手改 |
| `preset/芳乃预设.zip` | 同一份东西的压缩包（导入酒馆用得上） | 由脚本生成 |
| `preset/fano-thinking-chain.json` | 思维链折叠正则链（30 条，成品的配套件） | 别改 |
| `preset/build-report.txt` | 成品是怎么拼出来的（构建报告） | 只读 |
| `panel/fano-panel.js` | **面板脚本成品**（悬浮窗，92 KB，v0.4.0） | 别改 |
| `panel/preview.html` | 离线看面板：双击，用假酒馆 API 真跑面板本体 | 只读 |
| `panel/preview-host.js` | 离线预览用的假酒馆 API | 由脚本生成 |
| `panel/src/panel-core.js` | 面板的源码（成品由它构建出来） | 想改面板就改这里 |
| `panel/README.md` | 面板自己的说明（结构、配色、接口） | 只读 |
| `spec/*.json` | 构建与校验用的规格（分组、各层条目清单） | 别改 |
| `spec/SPEC.md`、`spec/生成器设计.md` | 这个工程的设计文档与踩坑记录 | 只读 |
| `tools/*.mjs` | 命令行脚本：自检、校验、打包 | 别改 |
| `包内容清单.txt` | 打包时生成的清单（每个文件多少字节） | 由脚本生成 |

**一句话原则**：`preset/`、`panel/fano-panel.js`、`tools/gui/lib/` 都是**产物或库**，要用就重新跑命令生成，
别手改（手改之后校验会报"和规格对不上"）。

## 3. 界面导览（左栏 23 项）

| 分组 | 视图 | 什么时候点它 |
|---|---|---|
| 从这里开始 | 引导：我要做什么 | **第一次来先点这个** |
| | 面板搭建（可视化） | 在面板的真样式上点着搭：加功能区/功能项/功能选项、就地改正文 |
| M0 解析 | 概览 / 诊断 / 条目 / 槽位占用 / 变量总线 / 依赖关系 / 标签族 / 分组建议 | 想搞清楚"这份预设到底长什么样、谁引用了谁、哪里在抢同一个槽位" |
| M1 拼装 | 拼装渲染 | 看**真正会发给模型的上下文**长什么样、有多少字、大概多少 token |
| | 变量轨迹 | 变量 `setvar/getvar` 按顺序跑一遍，谁先谁后、哪一步读到了空 |
| M2 体检 | 体检 / 待办 | 一张待办清单：必改 / 建议 / 提示，每条都写"为什么"和"怎么改" |
| M3 编辑 | 框架编辑 | 改成结构：增删条目、改名、开关、排序、换注入位置 |
| | 导出新预设 | 导出前的最后一道闸：改动摘要 + 导出前检查 + 来源完整性自证 |
| M4 新建 | 从零搭一份 | 手上什么都没有：让工具给你一副"能用但空"的骨架 |
| M5 正则 · 脚本 · 外观 | 正则编辑 | 改酒馆正则（左边原文、右边渲染出来的样子，可试跑、可看匹配数） |
| | 脚本编辑 | 看/改预设里的酒馆助手脚本（含语法检查与逐行 diff） |
| | 面板外观 | **改面板**：标题、颜色、输入框底色、悬浮球、窗口尺寸、壁纸 |
| M6 面板分组 | 面板分组 | 决定面板上出现哪些模块、每个模块管哪些条目（可按这份预设自动推断） |
| 附件 | 外部依赖 / 正则 / 内嵌脚本 | 只看不改的三个清单：外部依赖、正则、脚本 |

## 4. 五条常见路线

### 4.1 只想看看自己的预设有没有问题
导入预设 → **M2 体检 / 待办**（或左栏第二组的「诊断」看更细的结构问题）。
点「下载清单 .md」能把待办存成一份 Markdown 带走。

### 4.2 改提示词 / 开关条目 / 换顺序，然后导出
1. **M0 → 条目** 看一眼全貌（哪些开启、谁是"选项"、谁在抢槽位）；
2. **M3 → 框架编辑** 改结构；想直接改正文就在 **面板搭建** 的画布上点某条 → 右边就地改；
3. **M3 → 导出新预设**：先看「改动摘要」和「导出前检查」，绿了就点 **导出新预设 JSON**；
4. 导出的是一份**完整的新预设**（不是补丁）。

> **别人的原文一个字都不会被改**：没动过的条目在导出时会做逐字节自证；
> 你显式改过 / 删掉 / 自己新写的条目会被单独列出来，不含在"来源"里。

### 4.3 给自己的预设（已经带面板）改外观
1. 导入预设 → **M5 → 面板外观**；
2. 第一张卡是 **面板标题**（面板顶上那行字）；留空 = 用这份预设的名字；
3. 颜色表里带 ★ 的是最常改的两个：**窗口底色**、**输入框底色**；带透明度的项给的是"取色器 + 透明度滑杆"；
4. 右边的预览栏用的是**面板自己的 CSS**，所以调什么、装进酒馆就是什么；
5. 点 **应用到面板脚本** → 去 **M3 → 导出新预设**。

### 4.4 给一份"没有面板"的预设配一个面板
1. 导入那份预设；
2. **面板搭建**：工具会**按这份预设自动推断**一版模块（哪些是"选一"、哪些能叠加），你在画布上改；
3. **面板外观** 调颜色、改标题；
4. **装进这份预设** —— 面板脚本被写进预设的 `extensions.tavern_helper.scripts`；
5. **M3 → 导出新预设**。

> **记不住这一步也没关系**：搭过面板却没装进预设、或者改了还没应用，导出会被**拦住**并给一键补救；
> 反过来，这份预设本来就没有面板（只是改条目）则完全不打扰。

### 4.5 从零搭一份新预设
**M4 → 从零搭一份**：勾模块 → 生成骨架（注入位齐全、条目留空标「待填」、默认关着）→
在 **面板搭建** 里一条条写正文 → **M3** 导出。工具**不替你写提示词正文**，一句都不写。

## 5. 导出与安装到酒馆

| 你要的东西 | 在哪儿点 | 得到什么 | 怎么装 |
|---|---|---|---|
| 完整预设 | 顶栏 **导出预设 JSON** / M3 导出 | `<原名>-改.json` | 酒馆：预设（Completion Presets）→ 导入 |
| 只带走面板 | M5 面板外观 → **导出面板脚本 .js** | `fano-panel-外观定制.js` | 酒馆助手（JS-Slash-Runner）→ 脚本 → 新建 → 把内容粘进去 |
| 面板先看效果 | M5 → **导出独立预览页** | `fano-panel-预览.html` | 双击就能看：假酒馆 API + 真面板本体 |
| 面板直接看 | `panel/preview.html` | —— | 双击 |
| 重置值 | 各页的「清掉 / 回到出厂」按钮 | —— | 留空 = 用出厂值，不是"设成透明" |

**预设里带脚本的两种方式**：本工程的成品预设**自带**面板脚本（`extensions.tavern_helper.scripts`），
所以导入预设就等于装好了面板；如果你是在别人的预设上加面板，注意别装**两份**面板脚本——
两份会同时跑，桌面上会出现两个悬浮球（工具会拒绝装第二份并提醒你）。

## 6. 自检与故障排查

装完先跑这一条（包内自检，不需要 Chrome）：

```powershell
node tools/selftest.mjs        # 库能不能加载、成品对不对、zip 与 json 是否同源
node tools/check-preset.mjs    # 成品预设的权威校验（108 项）
```

要跑得更彻底（需要本机有 Chrome / Edge）：

```powershell
node tools/test-panel.mjs          # 面板逻辑（假 DOM，120 项）
node tools/test-panelconfig.mjs    # 面板外观配置 / 分组覆盖 / 壁纸 / 颜色不空转（83 项）
node tools/test-mobile.mjs         # 手机场景（20 项）
node tools/test-iframe.mjs         # iframe 嵌套与加载时序（26 项）
node tools/test-regex.mjs          # 思维链折叠链（30 项）
node tools/check-browser.mjs       # 真 Chrome · 面板几何与可点击性（58 项）
node tools/check-gui-browser.mjs   # 真 Chrome · 编辑器十屏逐屏数 DOM（198 项）
```

| 现象 | 原因 / 怎么办 |
|---|---|
| 页面白屏、左栏空白 | 多半是 `lib/*.js` 没加载（`file://` 下少一个文件就这样）。刷新 / 检查 `lib/` 是否齐全；按 F12 看 Console |
| 改了颜色没反应 | 先看右边的**预览栏**有没有跟着变：变了说明改的是面板；没变说明你改的是编辑器自己的界面（那是 `gui.css`，与预设无关） |
| 导出被拦住 | 看「导出前检查」那几条：待填却开着 / 面板没装进预设 / 面板改动还没应用，每条都写了怎么修 |
| 面板装进酒馆不出现 | 1) 面板脚本是不是没启用（`enabled: false`）；2) 酒馆助手装了没；3) 面板里 `window.__FANO_PANEL__.diagnose()` 会说明原因 |
| 导出的预设里没有面板 | 面板是**脚本**、条目是 `prompts`，两条路。导出前检查会拦住这种情况；补救按钮就在导出页第一张卡 |
| 面板脚本里我自己写的注释没了 | 工具只替换两个标记块**之间**的字面量，而那段会被重新序列化成 JSON——块**内**的注释留不住，块**外**的注释一个都不会动。想留注释就写在标记行外面 |
| 中文文档乱码 | 别用 PowerShell 的 `Get-Content`/`Set-Content` 改这些文件（按 ANSI 读、还加 BOM）。用编辑器改 |

## 7. 这个工具不做什么

- **不替你写提示词正文。** 新增条目一律"待填 + 默认关"，没写就开着会被拦住导出。
- **不改别人的原文。** 导入的预设里没动过的条目，导出时逐字节自证；你的改动分开记账。
- **不评测破甲效果。** 它只保证结构：槽位、顺序、引用、变量时序、面板够不够得着每一条。
  破甲强度取决于渠道、账号、角色卡和上下文，得在真实环境里试。
- **不整页换酒馆背景。** 那是酒馆主题/插件的活（`beautiCode` 那类）。这里的"壁纸"只作用于**面板自己**。
- **不在成品面板里放"搭建"功能。** 搭建只在编辑器里；装进预设的面板只有配置与分组两段可被替换。

---

# 第二部分 · 给 AI 的接口手册

> 目标：读完这一节，你可以直接调用这个工具里**全部的业务逻辑**（解析、体检、编辑、导出、
> 面板配置、分组推断、骨架生成），不必打开网页、不必重写任何规则。
> 下面是"能跑的最小知识"；**每个函数的完整签名与数据结构在 `tools/gui/API.md`**（同目录库文件的自带文档）。

## 8.1 模块总览

8 个库文件都是**普通脚本**（不是 ES module）：每个都是 IIFE，往 `globalThis` 上挂**一个**全局。

| 文件 | 全局名 | 干什么 | 对外函数数 |
|---|---|---|---|
| `lib/parse.js` | `PresetParse` | 预设 JSON → 派生模型（条目/槽位/变量/依赖/标签族/建议/正则/脚本/诊断） | 7 |
| `lib/assemble.js` | `PresetAssemble` | 按顺序拼装成"真正会发出去的上下文"，含变量展开与 token 估算 | 4 |
| `lib/invariants.js` | `PresetInvariants` | 体检：必改 / 建议 / 提示，每条带"为什么 + 怎么改 + 依据" | 4 |
| `lib/editor.js` | `PresetEditor` | 编辑态与导出：改正文/开关/顺序/增删、正则、脚本、导出前检查、来源自证 | 51 |
| `lib/skeleton.js` | `PresetSkeleton` | 从零生成一副骨架（注入位齐全、正文留空待填） | 10 |
| `lib/panelconfig.js` | `PresetPanelConfig` | 面板脚本里的配置块/分组块：读、写、夹取、校验、颜色 token 表 | 28 |
| `lib/groupinfer.js` | `PresetGroupInfer` | 按一份预设推断面板模块（互斥 / 叠加 / 只读 / 自己填 / 标签族…） | 4 |
| `lib/buildops.js` | `PresetBuildOps` | 可视化搭建的模型操作 + 面板质检（条目不存在、重名、空功能区…） | 23 |
| `lib/ui.js` | —— | **DOM 视图层**（23 个视图、拖拽、画布）。只在浏览器里能跑，不是编程接口 | —— |

## 8.2 怎么把代码加载起来

**Node（本工程的脚本与测试都用这一种）**：

```js
import fs from 'node:fs';
const load = (rel) => new Function('globalThis', fs.readFileSync(rel, 'utf8'))(globalThis);
for (const f of ['parse', 'assemble', 'invariants', 'editor', 'skeleton', 'panelconfig', 'groupinfer', 'buildops']) {
  load(`tools/gui/lib/${f}.js`);
}
const { PresetParse: PP, PresetEditor: PE, PresetPanelConfig: PC } = globalThis;
```

`new Function` 而不是 `import`：这些文件不是 ES module，靠 IIFE 挂全局；用 `new Function` 就能在
`globalThis` 作用域里把同一份源码跑一遍，得到完全一样的东西（`tools/selftest.mjs` 就是这么做的）。

**浏览器**：按 `index.html` 里的顺序 `<script src>` 即可（顺序：parse → assemble → invariants →
editor → skeleton → panelconfig → groupinfer → buildops）。它们都只用 `globalThis`、`JSON`、`Date`、
`Math`、`crypto.randomUUID`（没有就算了，有手写兜底），**不碰 DOM**——所以也能在 Worker 里用。

**先跑一遍自检**（确认环境没问题，并打印这份预设的关键数字）：

```powershell
node tools/selftest.mjs
```

## 8.3 必须遵守的六条不变式

违反其中任何一条，产物都会"看着正常、其实是坏的"：

1. **没动过的原文，导出必须逐字节相同。** `PE.emptyEdit(model, anchors)` + `PE.applyEdit(json, edit, model)`
   的结果与源 JSON `JSON.stringify` 完全相同（测试就是这么断言的）。别用"重新序列化整份 JSON"的方式改东西——
   要用编辑态，让没碰过的对象原样复用。
2. **注入位标记（marker）的正文必须真的是 0 字符，而且 `enabled: true`。** 它们是酒馆注入
   世界书/角色卡/人设/聊天记录的位置。给它塞注释或关掉它，对应内容就进不了上下文。
3. **新增条目默认关着。** 没写正文就开着 = 白占上下文，导出前检查会拦住（`待填却开着`）。
4. **`enabled` 有两份**：一份在 `prompts[i].enabled`，一份在 `prompt_order[0].order[j].enabled`。
   酒馆读**后者**。编辑器会在两处都写；你手写 JSON 时别忘了 `prompt_order`。
5. **面板脚本里只有"标记块"内的字面量可以被替换**：`FANO_PANEL_CONFIG_BEGIN/END`（外观配置）、
   `FANO_PANEL_GROUPS_BEGIN/END`（分组覆盖）。别整份重写面板脚本——那会丢掉别人的代码。
6. **面板标题永远不要写成空串。** 面板源码里有一句写死的兜底标题（`🌸 芳乃 · 预设面板`），
   留空就会显示它——装到别人的预设上就是"这个面板自称芳乃"。写的时候用 `panelTitle()` 的规则：
   你填了用你的，没填用**这份预设的名字**（源码里那句只在"预设自带的面板留空"时才该生效）。

## 8.4 八段可直接跑的配方

以下都从"已经按 8.2 加载好库"开始；第 3～5 段接着用第 1、2 段里的 `json / model / anchors / edit`。
路径按仓库根目录写。

```js
/* 1. 解析 + 打印全貌 */
import fs from 'node:fs';
const raw = fs.readFileSync('preset/芳乃预设.json', 'utf8');
const json = JSON.parse(raw);
const model = PP.parsePreset(json, '芳乃预设.json', Buffer.byteLength(raw));
console.log(model.counts, model.slots.length, model.variables.length, model.tagFamilies.length);
```

```js
/* 2. 体检 + 拼装（拿到"真正会发出去"的上下文与 token 估算） */
const asm = PA.assemble(model, { user: 'Master', char: '角色名' });
const rep = PI.check(model, asm);
console.log(`必改 ${rep.counts.err} · 建议 ${rep.counts.warn} · 提示 ${rep.counts.info}`);
fs.writeFileSync('待办.md', PI.toMarkdown(rep, model));       // 每条都带"为什么 + 怎么改"
```

```js
/* 3. 改一条正文并导出新预设（没碰过的条目逐字节不变） */
const anchors = model.entries.filter((e) => PS.MARKER_SLOT_IDS.includes(e.identifier)).map((e) => e.identifier);
const edit = PE.emptyEdit(model, anchors);
const target = model.entries.find((e) => e.listed && e.chars > 40);
const orig = PE.contentOf(edit, json, target.idx);            // 先记下原文，第 5 段要用
PE.setContent(edit, json, target.idx, orig + '\n（我加的一行）');
const out = PE.applyEdit(json, edit, model);
fs.writeFileSync('我的-改.json', JSON.stringify(out));
console.log('自证：', PE.verifySourceIntact(json, edit, model, PP.fingerprint));
```

```js
/* 4. 新增条目 + 打开它 + 导出前检查 */
const a = PE.addEntry(edit, model, { name: '我的新条目', slot: '' });   // 默认关着、正文待填
a.content = '这里是正文';
a.enabled = true;                                            // 开了才能进上下文
const checks = PE.exportChecks(json, edit, model, { anchorIds: anchors });
console.log(checks.blocking.length ? checks.blocking : '可以导出');
```

```js
/* 5. 不要了：删掉新增的 / 把改过的改回原文 */
PE.deleteAdded(edit, a.key);
PE.setContent(edit, json, target.idx, orig);                  // 改回原文 → 补丁自动撤销
console.log('回到原样：', JSON.stringify(PE.applyEdit(json, edit, model)) === JSON.stringify(json));
```

```js
/* 6. 读面板：配置、出厂配色、静态 CSS、兜底标题 */
const panelSrc = fs.readFileSync('panel/fano-panel.js', 'utf8');
const cfg = PC.extractConfig(panelSrc);        // 含 title / tokens / ball / window / layout / wallpaper
const themes = PC.extractThemes(panelSrc);     // 昼夜两套出厂色
const css = PC.extractCss(panelSrc);           // 面板静态 CSS（"真样式"预览用）
PC.TOKEN_SPEC.forEach((t) => console.log(t.key, t.label, t.type));
console.log('兜底标题：', PC.extractFallbackTitle(panelSrc));
```

```js
/* 7. 按一份预设推断面板模块 → 写进面板 → 装进预设 */
const inf = GI.inferGroups(model, json);
const draft = BO.seedFrom(inf);                       // 面板搭建用的可编辑草稿
const withPanel = PC.patchGroups(
  PC.patchConfig(panelSrc, { ...cfg, title: '这份预设的名字' }),
  BO.toOverride(draft),
);
const edit2 = PE.emptyEdit(model, anchors);
PE.addScript(edit2, json, { name: '我的预设 · 面板', content: withPanel, id: 'preset-panel' });
fs.writeFileSync('带面板-改.json', JSON.stringify(PE.applyEdit(json, edit2, model)));
```

```js
/* 8. 从零搭一份骨架 */
const sk = PS.buildSkeleton({ name: '我的预设', moduleIds: ['core', 'fano'], customCount: 3 });
fs.writeFileSync('骨架.json', JSON.stringify(sk.json));
console.log(sk.counts, sk.markers);            // 注入位齐全，正文留空待填
```

## 8.5 其余能力速查

- **正则**：`PE.regexViews(edit, json, model)` 读、`PE.setRegexField(...)` 写、`PE.testRegex(...)` 试跑、
  `PE.runRegexChain(rows, sample)` 看整条链跑完的结果。
- **脚本**：`PE.scriptViews / setScriptField / addScript / deleteScript / panelScriptViews / looksLikePanel`，
  `PE.checkScriptSyntax(content)` 只做编译检查（不执行）。
- **分组搭建**：`BO.addGroup / addMember / moveMember / setMode / removeSection / auditPanel / stats`，
  `BO.toOverride(draft)` 转成面板认的形状。
- **颜色**：`PC.clampConfig(cfg)` 夹取、`PC.toCssVars(clamped)` 生成 CSS 变量、
  `PC.validateConfig(cfg, themes)` 校验（会报"哪个 token 根本没被用到"）。
- **导出前检查**：`PE.exportChecks(json, edit, model, { anchorIds, panel })` 返回
  `{ blocking, warnings, notes }`；`panel` 传 `{ groups, appearance, unapplied, ignored }` 描述"这一轮搭没搭面板"。

## 8.6 命令与期望输出

| 命令 | 覆盖什么 | 期望 |
|---|---|---|
| `node tools/selftest.mjs` | 库加载、成品解析/体检/拼装、空编辑逐字节、骨架、面板配置与分组、zip 同源 | `失败 0 项` |
| `node tools/check-preset.mjs` | 成品预设的权威校验（规格、槽位、引用、原文未改、面板、zip） | `通过 108 项，失败 0 项` |
| `node tools/test-panel.mjs` | 面板逻辑（手风琴、互斥、一次写回、锚点保留） | `120 项` |
| `node tools/test-panelconfig.mjs` | 面板配置/分组/壁纸/颜色不空转/装进预设能真跑 | `83 项` |
| `node tools/test-mobile.mjs` | 手机视口（屏外坐标夹回、悬浮球可点） | `20 项` |
| `node tools/test-iframe.mjs` | iframe 嵌套、加载时序、悬浮球自愈 | `26 项` |
| `node tools/test-regex.mjs` | 思维链折叠链（块数 → 形态） | `30 项` |
| `node tools/check-browser.mjs [--mobile]` | 真 Chrome · 面板几何/遮挡/可点击 | `58 项` |
| `node tools/check-gui-browser.mjs [--mobile]` | 真 Chrome · 编辑器十屏 + 页面自检（页面内 198 项） | `通过 198 项` |
| `node tools/check-gui-browser.mjs`（查别的页面） | 用 `DSH_GUI_PAGE=<路径>` 指到任意 `index.html` | 同上 |
| `node tools/build-gui-demo.mjs` | 重建编辑器里的示例数据 | 生成 `demo/*.js` |
| `node tools/build-package.mjs` | 重新打包（`dist/芳乃预设生成器/` + zip） | 打印文件数与体积 |

页面内自检：打开编辑器时加 `?selftest=1`，跑完把结论写进页面标题（`GUI-SELFTEST-OK` / `FAIL`）
与 `#verdict`，并支持 `&view=<id>` 停在指定那一屏。

**改代码之后请重跑 `node tools/selftest.mjs` + `node tools/check-preset.mjs`**；
动了面板或编辑器再跑对应的真浏览器那两条。

## 8.7 边界（别指望它做的事）

- 它**不生成提示词内容**，也不判断破甲强度；它只保证结构正确、原文未改、面板够得着每一条。
- 它**不理解别人的 JS**：脚本编辑只做文本替换与语法检查，不会去分析逻辑。
- 面板里那两段标记块之外的代码，它**一个字符都不碰**。

# ── 本仓库只发工具与文档，不发任何预设正文 ────────────────────────────
#
# 为什么：这套工具是在真实的第三方预设上开发出来的，那些预设的正文属于各自的作者。
# 仓库里只保留"工具能跑"的最小集合；预设原文与它们的派生件一律不进版本库。
# 需要跑那些依赖预设的测试时，把你自己的样例放进 samples/（见 samples/README.md）。

# 源预设（他人的作品，仅开发环境使用）
/Izumi_0914.json
/Kemini_Dramatron_v3.1.json
/梦鲸思客V4-0915.json

# 成品预设与它派生的片段／压缩包
#
# 注意 fano-thinking-chain.json：它由 tools/build-regex.mjs 从两份源预设的
# regex replaceString（Izumi 与 Kemini 的思维链 HTML）改写而来——那 10k+ 字的
# HTML 是别人的东西，所以它和成品一样不进库。
/preset/芳乃预设.json
/preset/芳乃预设.zip
/preset/fano-thinking-chain.json
# 从九域一体化角色卡剥离出来的独立预设（预设正文 + 卡内控制台脚本，都属原作者）
/preset/九域0.2.7-预设.json
/芳乃预设_功能总览与使用说明/
*.7z

# 从源预设抽出来的清单（含原文）
/inventory/

# 适配器产物（tools/adapt-panel.mjs 输出的"另一支面板"适配副本）
# 里面是那份预设的完整正文（脚本 + 全部条目），属于各自作者，不进库。
*-适配.json

# 移植用的中间产物（tools/adapt-panel.mjs 从另一支脚本里抽出来的段落）
# 同理：那是别人的代码正文，只在本机当移植素材，不进库。
/_port/

# 构建产物
/dist/
/_zipcheck/
/panel/preview-host.js
/tools/gui/demo/*.js
# 但**标准纸**转出来的演示数据要随仓库走：克隆下来双击 index.html 就该有样例可看。
# 它由 tools/build-demo-paper.mjs 从 samples/标准纸.json 生成，可以用 --check 校验一致。
!/tools/gui/demo/paper-demo.js

# 夹具放这里；只保留说明文件
/samples/*
!/samples/README.md
# **标准纸**随仓库走（演示与测试都用它，clone 下来就该有）：它是我们自己的那张，
# 不是"你放进去的夹具"。其余东西照旧被上面那行挡着。
!/samples/标准纸.json

# 临时文件（跑测试/截图留下的）
*.log
_shot-*.png

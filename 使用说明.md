# 芳乃预设 · 使用说明

> 一页就够的版本。想看细节去 `README.md`，想看设计取舍去 `spec/SPEC.md`。

## 一分钟上手

1. 装好并启用 **酒馆助手（JS-Slash-Runner）** 扩展。
2. 酒馆 → 预设（Completion Presets）→ 导入 → 选 **`preset/芳乃预设.json`**。
3. 左下角出现圆形「芳」按钮，点开就是面板。面板脚本已内嵌在预设里，不用单独装。

> ⚠️ **成品只有一份，名字固定叫 `芳乃预设.json`**（早先带日期的那些已经删掉）。
> 带日期会堆出多份成品，导入时极容易拿错旧的那份——而旧那份内嵌的是旧面板，
> 于是出现"我明明改了、你却说还是不行"。
>
> **怎么确认导入的是哪一版**：看面板标题下方，或控制台里 `window.__FANO_PANEL__.diagnose().version`。
> 当前版本 **0.7.0**；显示成 `0.1.0` / `0.2.0` / `0.4.0` / `0.5.0` / `0.6.0` 就是旧预设。

预设概况：233 条（来源 228 + 本预设新增 5），初始开启 52 条，文件约 553 KB。
如果装过 **消息压缩（Chat Squash）**、**Kemini 面板**、**NoAss** 这类依赖预填充的插件，建议关掉。

## 这一版预设改了什么（面板 0.6.0 → 0.7.0）

> 版本怎么确认：酒馆里看面板标题下方，或控制台 `window.__FANO_PANEL__.diagnose().version`。
> 显示 `0.5.0` 或更早 = 你手里那份预设里的面板是旧版（见本节最后一条）。

**0.7.0 · 改正文的手势从长按改成三击**

| 对象 | 手势 |
|---|---|
| 条目正文 | **三击**任一条目 → 打开它的正文编辑器（改完点保存，和其它操作一样只写回一次） |
| 小方案的名字 | **三击** chip → 就地改名（回车保存、Esc 放弃） |
| 小方案本身 | **长按**或**右键** chip → 删除（长按那条是给手机的：那里点不出右键） |

- **代价**：三击与单击天生冲突，所以**单击动作要等一个窗口才生效**（默认 250ms，
  在生成器的「面板外观 → 交互」里可调 250–1500）。不等的话，三击的前两下会把条目开关两次、
  把小方案应用两次——而每一次都是一次写回。
- **连点两下不算三击**：按"点一下"处理（只切一次），不会切两次。点一下也不会误开编辑器。
- 0.5.0–0.6.0 那几版这里是**长按**；现在长按改成了"删除小方案"。

**0.6.0 · 顶部按钮 / 壁纸 / 小方案**

- 顶部那个按钮（文案 `🙈 隐藏`）不再是"开合窗口"，而是把**悬浮球和面板一起**藏起来、再点一下叫回来
  （隐藏状态会记住；Ctrl+Shift+F 也能切，控制台 `__FANO_PANEL__.show()` 是最后一条出路）。
- 标题栏多了 **🖼 壁纸开关**（只在配了壁纸时出现）：关掉壁纸后底色换成纯色、旁边出现 🎨 取色器；
  再点一下开回壁纸。这两样是**使用者偏好**，存本机、不进预设。
  "这份预设出厂时壁纸开不开"与"关掉后的纯色"（昼夜各一个）在生成器的「面板外观」里设。

**老面板会被拦住（重要）**

上面这些能力在面板源码里都有"能力标记"。预设里那份面板是旧版时，生成器的「导出新预设」会**红着拦住**，
并给两条路：「**换成新版面板**」与「就带这个旧面板导出」。

- **只改配置不换代码是不够的**：「应用到面板脚本」只替换 `CONFIG` 与 `GROUPS_OVERRIDE` 两段，
  面板代码一行不动。所以想让新手势生效，必须走「换成新版面板」（你改过的外观与分组会带过去）。
- 改完编辑器自己的文件后要按 **Ctrl+F5** 刷新页面：页面用的是它内存里那份面板快照，不刷新就是旧的。

## 面板怎么用

模块默认全部**折起**，点标题那一行展开。折起时标题右边仍写着当前状态（选了哪个文风、几项开着），不用展开就能看全。

改完任何选项**不会跳回顶部**；每次操作只写回预设一次。

| 分类 | 模块 | 形式 |
|---|---|---|
| 主体 | 芳乃主体层（3 条） | 开关排 |
| 核心机制 | **破甲（按模型分流）** | 骨架下拉框 + 该骨架的档位 |
| 核心机制 | **思维链 / 思考方式（13 选一）** | 下拉框 |
| 核心机制 | 思维链语言（5 选一） | 下拉框（只对 Izumi 系思考方式生效） |
| 内容 | 自定义内容（7 条） | 输入框 |
| 文风 | 主文风(17) · 第三人称追加(11) · 第一人称追加(4) · NSFW 文风(11) · 备选文风库(8) | 下拉框 / 开关排 |
| 叙事 | 人称(4) · 对白(4) · 难度(3) · 叙事与防护开关(32) | 下拉框 / 开关排 |
| 输出 | 输出模式(4) · 格式示例(5) · 摘要(4) · 字数(2) | 下拉框 |
| 其他 | NSFW(8) · 适配(5) · 前端美化(4) · 其他实验(18) | 开关排 |
| 只读 | 锚点(12) · 核心(17) | 不给开关，别动 |

## 破甲怎么选

先在最上面选**模型**，下面会自动换成那一套的档位。

| 模型 | 骨架 | 档位 |
|---|---|---|
| **Gemini** | Kemini 6 条 | 防截断（牢大/普通，可都不开）· NSFW 加强 · 雪融雪降填充 |
| **DeepSeek / GLM** | 梦鲸 1 条（`梦境思客`） | 渠道·思考标签（必选一）· 模型禁词 · V4Pro 小指标 |
| **其他模型** | Izumi 4 条 | 防截断（两条）· 破甲强度 · 尾部/预填充 |
| 手动 | 全关，自己开 | — |

**档位不会被切换模型清掉**，切走再切回来你之前调的还在。

## 思考方式怎么选

**思考方式决定模型吐几个思考块，块数决定显示成什么样**。

| 你选的 | 吐几块 | 显示成 |
|---|---|---|
| `📽️ICOT（三段）` | 3 块 | Kemini 形态（每块一个折叠条） |
| `📽️COT（格式友好型）` | 1 块 | Izumi 形态（毛玻璃卡片） |
| Izumi 系任一思维链 | 1 块 | Izumi 形态 |

折叠条上的文字统一是「**芳乃祈福中**」。

面板保证**同一时刻只有一套标签的思考条目开着**（Kemini 走 `<thinking>`、Izumi 走 `konatan_planning~`，两套混开会让模型同时吐两种）。组内不互斥。

## 必须你自己做的两件事

1. **`💠雪融雪降` 要换成你自己的文本。**
   面板 → 破甲 → Gemini → 「雪融雪降填充文本」→ 粘贴一段你自己的、**≥7000 token** 的私有小说段落 → 保存 → 打开它。
   不换会秒截断；用别人也会用的段落等于没换。默认是**关**的，等你去填。
2. **实测。** 本机没有酒馆、没有渠道 key。我只能保证静态校验全过；破甲强度取决于渠道、账号、角色卡和上下文，得你在真实环境按模型分别试。

## 常见问题

**聊天里冒出「当前主线任务 MQ.…／当前支线事件 SQ.…」这种文字？**
那是 `🏷新摘要` 在起作用。它原本配了 4 条正则（清理事件 + 压缩旧消息），这套配套没搬过来，所以那段文本直接漏到聊天里了。
**关掉方法**：面板 →「**摘要（选一）**」→ 选「**不使用**」。

**切换模型后世界书/角色卡上下文丢了？**
已修。那 12 条**锚点**（占 `worldInfoBefore/After`、`charDescription`、`main` 等注入位）以前被误放进了 Gemini 骨架，切走时被一起关掉了。现在它们是独立的只读模块，组装时强制开启。

**思维链显示成两种样子混在一起？**
已修。见上面「思考方式怎么选」。

**面板点标题展不开／改完跳回顶部？**
都已修。如果还遇到，先确认导入的是最新那份预设。

**MVU 变量不更新？**
面板 →「MVU 变量更新」→ 选「MVU（普通）」或「MVU Zod（JSONPatch）」。默认是「不使用」。

**手机上看不见面板／点悬浮球没反应？**
已针对手机修了五处（见下表）。**现在面板自己会播报**——如果它判断自己看不见，会用**酒馆自带的弹窗**（toastr，界面右上角）告诉你原因，手机上一眼就能看到，不需要控制台。
如果连播报都没有，说明脚本根本没在跑（检查酒馆助手扩展是否启用、脚本是否被关掉）。

| 已修的问题 | 原因 | 修法 |
|---|---|---|
| **悬浮球算到视口外**（实测 `(12,-158) / 视口 360×695`） | 悬浮球原先靠 `.fp-root{inset:0}` 撑满视口 + `bottom:96px` 定位。只要**根容器没被拉伸成视口大小**（页面某层祖先带了 `transform`/`filter`/`contain`，`position:fixed` 就会被关进那个祖先的坐标系），`bottom` 就是从零高度算出来的负值 | 悬浮球与窗口改成**自己 `position:fixed`**，坐标由 JS 按视口算好写进行内样式——**不再依赖容器尺寸，也不再依赖 `bottom`/`env()` 的解析**。画完还会量一次，跑飞了**自动改挂到 `documentElement`**（body 的兄弟）再试一次 |
| 挂在看不见的 iframe 里 | ①原实现只往上看**一层**父窗口；②更要命：解析 HOST 时要求父文档 `document.body` **已经就绪**。脚本执行只要稍早于父文档解析完，`body` 就是 `null` → 静默退回本地文档。桌面与手机加载时序不同，所以"桌面好、手机坏" | `HOST` 一路走到**最外层同源窗口**；不再要求 body 就绪，没就绪就**等**，绝不退回内层 |
| 打开了却看不见 | 存的坐标/尺寸是**桌面上留下的**，照用会把窗口整个推到屏外 | 每次渲染前把尺寸与坐标**夹回当前视口**；旋转/缩放后自动重新夹 |
| 出错后什么都不画（死寂） | `body.appendChild` 或样式注入抛异常 → 整个面板一个元素都不渲染，也没有任何提示 | 渲染前等 `body`；样式注入与 toast 各自容错；启动失败会播报 `lastError` |

**面板会自己播报**：启动后跑自检（跨域回退 / body 未就绪 / 根节点没插入 / 悬浮球没创建 / 悬浮球在视口外 / 读取预设出错），有问题就用**酒馆自带的弹窗**把原因播报出来（手机上一眼可见），正常时不打扰。

**卡住了想自救**：控制台跑 `window.__FANO_PANEL__.reset()` —— 清掉存的位置/尺寸/开关状态，把面板拉回默认样子。

**版本确认**：`window.__FANO_PANEL__.diagnose().version`。当前是 **0.7.0**。

## MVU 变量更新（一键开关）

面板 →「**MVU 变量更新**」→ 选一项就完事。

| 选项 | 用在 |
|---|---|
| **不使用** | 关掉变量更新（默认） |
| **MVU（普通）** | 你的 MVU 配置不带 Zod / JSONPatch |
| **MVU Zod（JSONPatch）** | 你的 MVU 带 Zod schema + JSONPatch |

两条互斥，一次只会开一条。**不需要再开别的模块**——骨架直接写在条目里。

**两个预设管不到的前提**：

- 真正把 `<UpdateVariable>` 解析并写进变量的是**酒馆助手里的 MVU / 变量更新扩展**。预设只负责让模型把块吐出来；没装那个扩展，模型吐了也没人接。
- 要更新哪些变量来自你**角色卡或世界书里的 schema**，预设里不含。

**输出位置**：两条骨架都明确要求把变量更新块放在**整轮回复的最末尾**（所有正文、格式内容、状态栏之后），且整轮只输出一次；没有变量要改时不输出。

**格式提醒**：默认的 `📽️ICOT（三段）` 要求输出分成三段，和"末尾追加一个块"的结构要求不完全一致。**开 MVU 时建议把思考方式换成 `📽️COT（格式友好型）` 或某个 Izumi 系思维链**。

> 早先的做法（Izumi 原设计）是把骨架塞进 `{{setvar::mvu::}}` 变量，再由「格式示例」里的 `{{getvar::mvu}}` 展开——生产者和消费者分散在两个模块，只开一半就等于没开。现在改成骨架字面写入，一键开关才真的是一键。**旧的 `✅MVU Zod兼容` 与 `✅MVU兼容（用再开）` 已从预设移除**，留着会形成第二套竞争机制。

## 想关掉某个效果时，去哪找

| 想关掉 | 去哪个模块 |
|---|---|
| 摘要 / 事件追踪那段文字 | 摘要（选一）→ 不使用 |
| 思维链 / 思考 | 思维链 → 不使用 |
| 吐槽 / 创作思路那一栏 | 输出模式（四选一）→ 不使用 |
| 防全知、防转折、抢话等 | 叙事与防护开关 |
| NSFW 指导 | NSFW 指导与加强 |
| 附加文风 | 第三人称/第一人称追加文风 |
| 前端美化（选项栏、弹幕） | 前端与美化 |

**锚点（12 条）和核心（17 条）不要关**——它们是槽位与结构，关掉会丢上下文或直接坏掉。

## 已知限制

- 切到非 Gemini 分支时，`main` / `jailbreak` 两个槽位里仍是 Kemini 的文本（槽位唯一，按规格归 Kemini）。
- `⭐️思维链语言-*` 只对 Izumi 系的思考方式有意义；走 ICOT/COT 时它不起作用。
- Izumi 原有 30 条美化/隐藏正则没搬（绑死它自己的标签与结构）。
- 面板只切**条目**，没做正则/脚本的运行时门控。
- 梦鲸只用它的破甲词，不含脚本管线；`{{压缩相邻消息::…}}` 宏没搬。

## 测试

```
node tools/selftest.mjs               #  70 项  包内自检（库加载 + 成品 + 空编辑逐字节 + 面板 + zip）
node tools/check-preset.mjs           # 109 项  成品独立校验
node tools/test-regex.mjs             #  30 项  折叠链（块数→形态）
node tools/test-panel.mjs             # 191 项  面板逻辑（假 DOM）
node tools/test-panelconfig.mjs       #  107 项  面板外观配置 + 分组覆盖 + 壁纸 + 颜色不空转
node tools/test-mobile.mjs            #  20 项  手机场景（390×844，屏外坐标会被夹回）
node tools/test-iframe.mjs            #  34 项  iframe 嵌套、加载时序、悬浮球自愈
node tools/check-browser.mjs          #  58 项  真浏览器 · 桌面视口
node tools/check-browser.mjs --mobile #  58 项  真浏览器 · 手机视口
node tools/test-gui.mjs               # 359 项  生成器内核（M0 → M6 + EJS + 搭建 + 导出前检查）
node tools/check-gui-browser.mjs      # 235 项  真浏览器 · 生成器 GUI（十屏）
```

**1104 项断言全绿**（需要 Chrome 的三条要更宽的沙箱权限）。

## 打包给别人的那份

```
node tools/build-package.mjs     # → dist/芳乃预设生成器/ 与 dist/芳乃预设生成器.zip
```

包里是编辑器 + 可编程的库 + 成品与源码 + **说明书.md**；示例素材只有芳乃预设这一份。
拿到包的人双击 `开始.html` 就能用。包里能独立跑的自检见上面表格里不需要第三方源预设的那几条。

`说明书.md` 有两部分：**第一部分给人看**（怎么打开、点哪里、五条常见路线、怎么装进酒馆、
故障排查表）；**第二部分给 AI 看**（8 个库的全局名与职责、Node/浏览器两种加载方式、
必须遵守的六条不变式、八段可直接复制运行的配方、命令与期望输出）。
每个函数的完整签名与数据结构在 `tools/gui/API.md`。

## 预设解析器 / 生成器（另一个工具，不是预设本身）

`tools/gui/index.html`，**双击就能打开**，零依赖、不联网、不改你的文件。

**第一次用就点左栏第一项「引导：我要做什么」** —— 那页按"你手上有没有预设、改到哪一步"给你一张清单，
每一步都写清做什么、为什么、做完长什么样，旁边直接跳过去。右上角常驻 **导入预设…**、**⟳ 刷新**、**导出预设 JSON**。

> **⟳ 刷新** 只重挂当前这一屏（不重载页面）：页面跑钝了用它解卡，导入的预设、编辑草稿、
> 输入框里打了一半的字都在。它不是浏览器刷新——改了编辑器的 JS 要生效仍旧按 Ctrl+F5。

导入任何酒馆预设 JSON，它会：

- **摊开结构**：条目、槽位占用与冲突、变量谁设谁读、标签族、外部宏、正则与脚本、分组建议；
- **按 `prompt_order` 真实拼一遍**：有状态地展开 `setvar/getvar/addvar`，
  告诉你每段最终会变成什么、正文多少字、大约多少 token；
- **体检**：给一份「必改 / 建议 / 提示」清单，每条都写清为什么、怎么改、依据是什么；
- **改结构再导出**：上移下移、开关、改条目名、增删条目，导出成一份完整的新预设文件；
- **从零搭一份**：勾模块生成骨架——条目名、槽位、顺序、开关都给你排好，
  **正文一个字都不代写**，全是你自己敲。位置标记（世界书/角色卡/人设/场景/示例/聊天记录）是空的但开着，
  那是酒馆塞东西的位置，不是漏填；
- **改正则**：启停、改名、改 find/替换、调顺序、增删，**当场贴一段样例试跑**看效果（还会告诉你一串正则跑完的结果）；
- **改脚本**：启停、改名、代码框、语法校验（只编译不执行）、和原文的 diff。改的是文本——
  任意 JS 不可能"可视化编辑"，这一条不吹；
- **改面板外观**：20 个颜色（白天/夜间各一套，含**输入框底色与输入框的边**）、悬浮球大小与字形、窗口尺寸与上下限、圆角、
  整体缩放、不透明度与磨砂、**面板壁纸**，以及**交互**里的「三击条目改正文」（开/关 + 三击间隔毫秒）——旁边就是实时预览，用的是面板真样式；
- **改面板分组**：**按当前预设推断模块**（哪些条目是"选一"、哪些能叠加、哪些只读、哪些要你填内容），
  推断结果能在界面上增删改；**把面板装进任意预设**——导入别人写的、本来没有面板的预设，
  推断分组 + 调外观，点「装进这份预设」就跟着预设一起导出。
- **三击条目改正文**（0.7.0 起；0.5.0–0.6.0 那几版是长按）：在酒馆里连点三下面板上任一条目就打开它的正文编辑器，改完点保存——
  只写回预设一次。导出前检查会盯住这件事：预设里那份面板是**旧版**（没这个能力）就拦住导出，
  卡上有「换成新版面板」（你改过的外观与分组会带过去）和「就带这个旧面板导出」两条路；
  预设自带的是**别人的**面板时只提醒一句，不拦、也不会去换人家的面板。
- **顶部按钮 = 隐藏/显示**（面板 0.6.0 起）：酒馆助手按钮区那个按钮（文案 `🙈 隐藏`）
  把**悬浮球和面板一起**藏起来、再点一下叫回来（隐藏状态会记住。Ctrl+Shift+F 也能切，
  控制台 `__FANO_PANEL__.show()` 是最后一条出路）。
- **壁纸开关 + 纯色背景**（面板 0.6.0 起）：配了壁纸的预设，标题栏多一个 🖼 按钮——
  点一下关掉壁纸、底色换成纯色，旁边出现取色器自己挑；再点一下开回壁纸。
  两样都能在生成器的「面板外观」里设成**这份预设的默认值**：「这份预设默认开着壁纸」那个勾，
  以及配色区里的「纯色底（关掉壁纸时）」（昼夜各一个）。用户在面板上点过之后以他点的为准。
- **小方案三击改名**（0.6.0 起；0.7.0 起手势改成三击）：脚注里的小方案 chip **三击就地改名**（回车保存、Esc 放弃），
  新存的小方案一样能改；**点一下仍是应用、长按或右键是删除**（长按那条是给手机的：那里点不出右键）。
- **旧面板会被拦住**（面板 0.6.0 起）：上面这些能力在面板源码里有能力标记，导出前检查会认它——
  预设里那份面板是 0.5.0 或更早（缺这些控件）时，导出会被拦住并给「换成新版面板」与
  「就带这个旧面板导出」两条路，免得装进酒馆"看着差不多、功能没有"。

想看清"为什么某个开关没生效""上下文为什么短了""这条正则到底干了什么"，比在酒馆里试快得多。
它只管结构与外观，**不替你写提示词内容**，也不会改别人原有的一个字（导出时逐条核对并给出结果）。

### 可视化搭建一份预设（左栏第二项「面板搭建」）

这一页把"看面板渲染"和"搭预设"合成一个地方：**左边是面板的渲染（真样式），右边是属性**。
三层：**分节 → 功能区 → 功能项**（功能项就是一个条目；选一型功能区里，每个条目就是一个选项）。

1. 导入一份预设（本来没有面板也行：画布会先显示面板自带的分组，或是一片空白等你搭）；
2. 点一个功能区 → 右边出现它的属性：改名、改形式（选一 / 可多选 / 要你自己填 / 只读）、换分区、删；
3. 点功能区里的 **＋功能项** → **真的新建一个条目**（正文留空标「待填」、默认关着）并挂进去；
   **＋功能选项** 是同一件事，只是加在选一型功能区里当另一个选项；
   想用已有的条目，用右边的「把已有条目挂进来」；
4. **点功能项** → 右边就能**直接改它的提示词正文**（导入来的原文也能改，改过会标「你改过」，随时能「还原成导入时的原文」）；
5. **点分节标题** → 右边改分节名、上移下移、加功能区、删（删了里面的功能区会挪到别的分节，不会丢条目）；
6. **拖功能区**就能换位置（拖到别的分区里＝搬家）；
7. 画布上方的**面板质检**会挑出"点了没反应 / 打开了也没用"的地方（挂着不存在的条目、条目重名、
   空功能区、默认开着但正文是空的……），每条都有「去看」；
8. 颜色 / 尺寸 / 壁纸在 **M5 面板外观**——同一份配置，切过去调完再回来；
9. 点 **装进这份预设 / 应用到面板脚本** → 再点右上角 **导出预设 JSON**。

> 改的都是**草稿**：面板自带的分组不会被碰，只有点了"应用/装进"才写进面板脚本。
> 想从零开始：引导页第四条路线「我什么都没有：从一个空面板开始」。

### 改面板外观 / 给别人的预设配一个面板

**给自己的预设（已经带面板）改外观：**

1. 导入 `preset/芳乃预设.json`（面板脚本在里面），左边进 **M5 → 面板外观**；
2. 调颜色 / 尺寸 / 壁纸，预览立刻跟着变（点「看夜间」切另一套配色）；
3. **面板标题**（第一张卡，就是面板顶上那行字）：留空 = 用这份预设的名字；
4. 点 **应用到面板脚本** —— 只替换脚本里那段配置，面板代码一行不动；
5. 去 **M3 → 导出新预设**，导出成文件，再导进酒馆。

**给一份"没有面板"的预设配上这个面板：**

1. 导入那份预设（比如别人写的）；
2. 去 **M6 → 面板分组** → 点 **按这份预设自动推断** → 逐个改模块名、增删条目；
   （面板是按**条目名**匹配的，推断结果里重名的条目会被排除并给出理由）
3. 去 **M5 → 面板外观** 调外观、改**面板标题**（预览里会显示推断出来的真实模块）；
4. 点 **装进这份预设** → 面板脚本就被写进预设的扩展里了；
5. **M3 → 导出新预设**，导出成文件，导进酒馆即可。

> **标题为什么必须写死成一个具体的字**：面板源码里有一句兜底文案（`🌸 芳乃 · 预设面板`）。
> 配置里的标题留空时，真面板显示的就是那一句——把面板装到别人的预设上，就成了"这个面板自称芳乃"。
> 所以生成器写进预设时一律把标题落成具体的字：你填了用你的，没填用**这份预设的名字**。
> （预设**自带**的面板留空则照旧用源码那句——那是面板作者的意图，画布也跟着它，两边不会两样。）

> 导出前检查会盯住这件事：搭了面板但文件里没有面板脚本、或改了还没应用，都会**拦住导出**并给一键补救；
> 这份预设本来就没面板（正常只改条目）则完全不打扰。

> 没导入任何预设也能玩：这时用的是内置面板模板，可以「导出面板脚本 .js」贴进酒馆助手，
> 或者「导出独立预览页」双击看真面板（跑的是面板本体）。
